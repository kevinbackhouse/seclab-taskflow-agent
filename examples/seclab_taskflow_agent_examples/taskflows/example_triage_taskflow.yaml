# SPDX-FileCopyrightText: 2025 GitHub
# SPDX-License-Identifier: MIT

# a simple example of the triage Agent pattern
seclab-taskflow-agent:
  version: 1
  filetype: taskflow

taskflow:
  - task:
      must_complete: true
      agents:
        - seclab_taskflow_agent.personalities.assistant
      user_prompt: |
        Store the json array ["apples", "oranges", "bananas"] in the `fruits` memory key.
      env:
        MEMCACHE_STATE_DIR: "example_taskflow/"
        MEMCACHE_BACKEND: "dictionary_file"
      toolboxes:
        - seclab_taskflow_agent.toolboxes.memcache
  - task:
      must_complete: true
      agents:
        - seclab_taskflow_agent.personalities.assistant
      user_prompt: |
        Retrieve the contents of the `fruits` memory key.
      env:
        MEMCACHE_STATE_DIR: "example_taskflow/"
        MEMCACHE_BACKEND: "dictionary_file"
      toolboxes:
        - seclab_taskflow_agent.toolboxes.memcache
  - task:
      model: gpt-4.1
      repeat_prompt: true
      agents:
        # primary agent for this task
        - seclab_taskflow_agent_examples.personalities.example_triage_agent
        # handoff agents
        - seclab_taskflow_agent_examples.personalities.apple_expert
        - seclab_taskflow_agent_examples.personalities.orange_expert
        - seclab_taskflow_agent_examples.personalities.banana_expert
      user_prompt: |
        Tell me more about how {{ RESULT }} are grown.
